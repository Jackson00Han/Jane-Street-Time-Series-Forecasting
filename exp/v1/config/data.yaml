blob:
  container: "jackson"
  prefix: "js_exp"
local:
  root: "/mnt/data/js"

exp_root: "exp/v1"   # 只在这里写一次版本

paths:
  raw:        "raw"
  clean:      "clean" # in exp_root
  raw_shards: "raw_shards" # in exp_root
  fe_shards:  "fe_shards" # in exp_root
  models:     "models" # in exp_root
  reports:    "reports" # in exp_root
  # 本地专用
  mm:         "mm"
  cache:      "cache"
  tmp:        "tmp"

# 关键列与时间范围
keys: ["symbol_id","date_id","time_id"]
target: "responder_6"
weight: "weight"
date_lo: 1190
date_hi: 1429
embargo_days: 3

test_lo: 1530